version: 2

base-dbt-profile:
  target: dbt
  outputs:
    dbt:
      threads: 4
      type: bigquery
      method: service-account
      keyfile: "{{ env_var('DBT_GOOGLE_KEYFILE') }}"
      project: striped-torus-445510-c9
      schema: dbt
      location: US